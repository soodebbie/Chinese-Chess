services:
  - type: web
    name: chinese-chess
    env: python
    buildCommand: pip install -r requirements.txt && pip install .
    startCommand: HOST=0.0.0.0 PORT=$PORT python -m chinese_chess.web
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
